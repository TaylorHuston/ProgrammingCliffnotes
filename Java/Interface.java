//Interfaces cannot be instantiated on their own, they define a set of methods that any classes that use it MUST implement
public interface Interface {
  public int getInt(); //Any classes that implements this must have this method
}
